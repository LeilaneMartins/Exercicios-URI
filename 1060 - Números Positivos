#include <stdio.h>
 
int main() {
 
    float x, cont;
    int aux = 0;

    for(cont =1; cont <=6; cont++){
        scanf("%f",&x);

        if(x > 0){
            aux++;
        }
    }

    printf("%i valores positivos\n",aux);
 
    return 0;
}
