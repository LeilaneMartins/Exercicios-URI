#include <stdio.h>
 
int main() {
 
    int x, y, cont;

    scanf("%i%i",&x,&y);


    if(x <= y){
        for(cont = x+1; cont < y; cont++){
            if(cont % 5 == 2 || cont % 5 == 3){
                printf("%i\n",cont);
            }
        }
    }
    else{
        for(cont = y+1; cont < x; cont++){
            if(cont % 5 == 2 || cont % 5 == 3){
                printf("%i\n",cont);
            }
        }
    }
 
    return 0;
}
